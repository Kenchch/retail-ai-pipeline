[pytest]
# The package is installed (`pip install -e .`), so no sys.path manipulation is
# needed here. A bare `pytest` and `python -m pytest` now behave the same, which
# is what the README's command depends on.
testpaths = tests
